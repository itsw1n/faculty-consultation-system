'use client'
import { useCallback,useEffect,useState } from 'react'
import { usePathname,useRouter,useSearchParams } from 'next/navigation'
import { SearchInput } from '@/components/common/SearchInput'
export function ApplicationFilters({departments}:{departments:{id:string;code:string}[]}){const current=useSearchParams();const pathname=usePathname();const router=useRouter();const[search,setSearch]=useState(current.get('search')??'');const update=useCallback((name:string,value:string)=>{const params=new URLSearchParams(current);if(value)params.set(name,value);else params.delete(name);router.replace(`${pathname}?${params}`)},[current,pathname,router]);useEffect(()=>{const timeout=setTimeout(()=>{if(search!==(current.get('search')??''))update('search',search)},300);return()=>clearTimeout(timeout)},[search,current,update]);const label='grid flex-1 gap-1.5 text-xs font-bold';const select='min-h-11 rounded-lg border border-border bg-surface px-3 outline-none focus-visible:ring-3 focus-visible:ring-focus';return <div className="grid items-end gap-3 rounded-xl border border-border bg-surface p-4 sm:grid-cols-2 lg:flex"><SearchInput label="Search" value={search} onChange={setSearch} placeholder="Search name or email"/><label className={label}><span>Status</span><select className={select} value={current.get('status')??'PENDING'} onChange={event=>update('status',event.target.value)}><option value="PENDING">Pending</option><option value="APPROVED">Approved</option><option value="REJECTED">Rejected</option><option value="">All</option></select></label><label className={label}><span>Role</span><select className={select} value={current.get('role')??''} onChange={event=>update('role',event.target.value)}><option value="">All</option><option value="STUDENT">Student</option><option value="FACULTY">Faculty</option></select></label><label className={label}><span>Department</span><select className={select} value={current.get('department')??''} onChange={event=>update('department',event.target.value)}><option value="">All</option>{departments.map(item=><option value={item.id} key={item.id}>{item.code}</option>)}</select></label></div>}
