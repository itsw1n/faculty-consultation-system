# Authentication Flow

Authentication is intentionally undecided. No login or pretend role state is generated. The client starts without a user session; any remote service remains responsible for access control.

When auth is added, document login, storage, expiry, refresh ownership, logout, recovery, request-integrity boundaries, and resource authorization here. Do not rerun the generator over this project.
