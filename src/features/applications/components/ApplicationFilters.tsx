'use client'
import { useCallback,useEffect,useState } from 'react'
import { usePathname,useRouter,useSearchParams } from 'next/navigation'
import { SearchInput } from '@/components/common/SearchInput'
export function ApplicationFilters({departments}:{departments:{id:string;code:string}[]}){const current=useSearchParams();const pathname=usePathname();const router=useRouter();const[search,setSearch]=useState(current.get('search')??'');const update=useCallback((name:string,value:string)=>{const params=new URLSearchParams(current);if(value)params.set(name,value);else params.delete(name);router.replace(`${pathname}?${params}`)},[current,pathname,router]);useEffect(()=>{const timeout=setTimeout(()=>{if(search!==(current.get('search')??''))update('search',search)},300);return()=>clearTimeout(timeout)},[search,current,update]);return <div className="filter-bar"><SearchInput label="Search" value={search} onChange={setSearch} placeholder="Search name or email"/><label><span>Status</span><select value={current.get('status')??'PENDING'} onChange={event=>update('status',event.target.value)}><option value="PENDING">Pending</option><option value="APPROVED">Approved</option><option value="REJECTED">Rejected</option><option value="">All</option></select></label><label><span>Role</span><select value={current.get('role')??''} onChange={event=>update('role',event.target.value)}><option value="">All</option><option value="STUDENT">Student</option><option value="FACULTY">Faculty</option></select></label><label><span>Department</span><select value={current.get('department')??''} onChange={event=>update('department',event.target.value)}><option value="">All</option>{departments.map(item=><option value={item.id} key={item.id}>{item.code}</option>)}</select></label></div>}
