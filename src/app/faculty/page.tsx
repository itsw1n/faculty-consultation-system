import Link from 'next/link'
import { EmptyState } from '@/components/common/EmptyState'
import { PageHeading } from '@/components/common/PageHeading'
import { getFacultyDashboard } from '@/features/dashboard/repositories/dashboardRepository'
export default async function FacultyDashboard() { const data=await getFacultyDashboard();return <><PageHeading title="Faculty Dashboard" description="Manage availability and consultation requests."/><div className="dashboard-grid"><section className="summary-card"><span>Pending requests</span><strong>{data.pending}</strong><p>Requests needing a decision</p></section><section className="summary-card"><span>Upcoming</span><strong>{data.upcoming}</strong><p>Approved consultations</p></section><section className="summary-card"><span>Open slots</span><strong>{data.openSlots}</strong><p>Available consultation times</p></section></div>{data.openSlots===0&&<EmptyState title="No availability added" description="Add an available time so students can request a consultation." action={<Link className="inline-action" href="/faculty/availability">Add Availability</Link>}/>}</> }
