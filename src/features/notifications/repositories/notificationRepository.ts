import 'server-only'
import { createClient } from '@/lib/supabase/server'
import type { NotificationItem } from '../types'
export async function listNotifications(limit=20){const{data,error}=await(await createClient()).from('notifications').select('id,type,title,message,read_at,created_at').order('created_at',{ascending:false}).limit(limit);if(error)throw new Error('Unable to load notifications');return data as NotificationItem[]}
export async function getNotificationPage(cursor?:{createdAt:string;id:string}){let query=(await createClient()).from('notifications').select('id,type,title,message,read_at,created_at').order('created_at',{ascending:false}).order('id',{ascending:false}).limit(20);if(cursor)query=query.or(`created_at.lt.${cursor.createdAt},and(created_at.eq.${cursor.createdAt},id.lt.${cursor.id})`);const{data,error}=await query;if(error)throw new Error('Unable to load notifications');const items=data as NotificationItem[];const last=items.at(-1);return{items,hasMore:items.length===20,nextCursor:last?{createdAt:last.created_at,id:last.id}:null}}
