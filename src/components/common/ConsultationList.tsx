import type { ReactNode } from 'react'
import type { ConsultationItem } from '@/features/consultations/types'
import { StatusBadge } from './StatusBadge'
export function ConsultationList({items,person,actions}:{items:ConsultationItem[];person:'student'|'faculty';actions?:(item:ConsultationItem)=>ReactNode}){if(!items.length)return <p className="p-8 text-center text-muted">No consultations found.</p>;return <section className="overflow-hidden rounded-xl border border-border bg-surface">{items.map(item=><article className="grid items-center gap-4 border-b border-border p-4 last:border-0 sm:grid-cols-2 lg:grid-cols-[2fr_1fr_.8fr_auto]" key={item.id}><div><strong>{person==='student'?item.student_name:item.faculty_name}</strong><p className="mt-1 text-muted">{item.purpose}</p></div><div><time>{item.slot_date}</time><p className="mt-1 text-muted">{item.start_time.slice(0,5)}–{item.end_time.slice(0,5)}</p></div><StatusBadge status={item.consultation_status}/>{actions?.(item)}</article>)}</section>}
