'use client'
import { reviewApplication, reviewApplications } from '../actions'
type Item={id:string;full_name:string;email:string;requested_role:string|null;account_status:string|null;departments:{name:string}[]}
export function ApplicationReviewList({applications}:{applications:Item[]}){return <><form id="bulk-review" action={reviewApplications} onSubmit={event=>{const submitter=(event.nativeEvent as SubmitEvent).submitter as HTMLButtonElement|null;if(!window.confirm(`${submitter?.value==='APPROVED'?'Approve':'Reject'} the selected applications?`))event.preventDefault()}}/><div className="bulk-actions"><span>Select pending applications below</span><button form="bulk-review" name="decision" value="APPROVED">Approve selected</button><button form="bulk-review" className="danger-action" name="decision" value="REJECTED">Reject selected</button></div><div className="data-list">{applications.length===0?<p className="empty-row">No applications match these filters.</p>:applications.map(application=><article className="application-row" key={application.id}>{application.account_status==='PENDING'?<input form="bulk-review" aria-label={`Select ${application.full_name}`} type="checkbox" name="userIds" value={application.id}/>:<span/>}<div><strong>{application.full_name}</strong><p>{application.email}</p></div><div><span className="status-badge">{application.requested_role??application.account_status}</span><p>{application.departments?.[0]?.name??'No department'}</p></div>{application.account_status==='PENDING'&&<form action={reviewApplication} className="row-actions"><input type="hidden" name="userId" value={application.id}/><button name="decision" value="APPROVED">Approve</button><button className="danger-action" name="decision" value="REJECTED">Reject</button></form>}</article>)}</div></>}
