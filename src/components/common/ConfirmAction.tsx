'use client'
import { Button,Dialog,DialogTrigger,Heading,Modal,ModalOverlay } from 'react-aria-components'
export function ConfirmAction({label,title,description,fields,submitAction,variant='primary'}:{label:string;title:string;description:string;fields:Record<string,string>;submitAction:(formData:FormData)=>Promise<void>;variant?:'primary'|'danger'}){return <DialogTrigger><Button className={`confirm-trigger ${variant}`}>{label}</Button><ModalOverlay className="modal-overlay" isDismissable><Modal className="confirm-modal"><Dialog role="alertdialog">{({close})=><><Heading slot="title">{title}</Heading><p>{description}</p><form action={submitAction}>{Object.entries(fields).map(([name,value])=><input key={name} type="hidden" name={name} value={value}/>)}<div className="dialog-actions"><Button type="button" onPress={close}>Go back</Button><Button type="submit" className={variant}>{label}</Button></div></form></>}</Dialog></Modal></ModalOverlay></DialogTrigger>}
